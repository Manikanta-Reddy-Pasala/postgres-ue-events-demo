spring.datasource.url=jdbc:postgresql://postgres:5432/uetracker?reWriteBatchedInserts=true
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hikari sized for concurrent generate + read
spring.datasource.hikari.maximum-pool-size=16
spring.datasource.hikari.minimum-idle=4

# CQRS read side lives on a SEPARATE Postgres (physical read/write isolation).
# Holds cqrs_read_* tables; projector writes here, CQRS reads come from here.
app.read.datasource.url=jdbc:postgresql://postgres-read:5432/uetracker?reWriteBatchedInserts=true
app.read.datasource.username=postgres
app.read.datasource.password=postgres
app.read.datasource.hikari.maximum-pool-size=16
app.read.datasource.hikari.minimum-idle=4

# Flyway owns schema; JPA only validates
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.jpa.show-sql=false

# Projector cadence (ms)
benchmark.projector.fixed-delay=1000
benchmark.projector.batch-size=2000
benchmark.generation.chunk-size=50000
